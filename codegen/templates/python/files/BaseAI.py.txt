# -*- python -*-

from library import library

class BaseAI:
%for model in models:
    ${lowercase(model.name)}s = []
%endfor

    def startTurn(self):
%for model in models:
        from GameObjects import ${model.name}
%endfor

%for model in models:
        BaseAI.${lowercase(model.name)}s = [${model.name}(library.get${model.name}s(i)) for i in xrange(library.get${model.name}Count())]
%endfor

        if self.turnNum() == 1:
            self.init()
        self.run()
    
%for datum in globals:
    @staticmethod
    def ${datum.name}():
        return library.get${capitalize(datum.name)}()

%endfor
